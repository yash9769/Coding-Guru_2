// Test file to check if aliases are working
import { Button } from "@/components/ui/button";
import { useAuth } from "@/hooks/useAuth";
import Dashboard from "@/pages/dashboard";

console.log("Alias test:", Button, useAuth, Dashboard);
